{
  "name": "somethings",
  "configuration": {
    "integrationType": "ChirpStack",
    "isDevice": true,
    "name": "Device ${eui}",
    "profile": "${deviceProfileName}",
    "customer": null,
    "group": null,
    "attributes": [
      "eui",
      "devAddr",
      "fPort",
      "applicationId",
      "applicationName",
      "tenantId",
      "tenantName",
      "deviceProfileId",
      "deviceProfileName",
      "frequency",
      "bandwidth",
      "spreadingFactor",
      "codeRate"
    ],
    "timeSeries": [
      "temperature",
      "humidity",
      "light",
      "pir",
      "vdd"
    ],
    "scriptLang": "TBEL",
    "customFunction": "function decodePayload(input) {var output = {attributes: {},telemetry: []};// --- Decoding code --- //var decoded = {};for (var i = 0; i < input.length;) {var type = input[i++] & 0xff;if (type == 0x01) { // Temperature Converts to °Cdecoded.temperature = parseBytesToInt(input, i, 2, true) / 10;i += 2;} else if (type == 0x02) { //Humiditydecoded.humidity = input[i];i += 1;} else if (type == 0x04) { // Lightdecoded.light = parseBytesToInt(input, i, 2, true);i += 2;} else if (type == 0x05) { // Motion (PIR)decoded.pir = input[i];i += 1;} else if (type == 0x07) { // Battery Voltage (VDD)decoded.vdd = parseBytesToInt(input, i, 2, true);   i += 2;}}output.telemetry = [{ts: timestamp,values: decoded}];// --- Decoding code --- //return output;}",
    "updateOnlyKeys": [
      "tenantId",
      "tenantName",
      "applicationId",
      "applicationName",
      "deviceProfileId",
      "deviceProfileName",
      "devAddr",
      "fPort",
      "frequency",
      "bandwidth",
      "spreadingFactor",
      "codeRate",
      "vdd",
      "channel",
      "rfChain",
      "eui"
    ],
    "keys": [
      "deduplicationId",
      "time",
      "deviceInfo",
      "devAddr",
      "adr",
      "dr",
      "fCnt",
      "fPort",
      "confirmed",
      "data",
      "rxInfo",
      "txInfo",
      "tenantId",
      "tenantName",
      "applicationId",
      "applicationName",
      "deviceProfileId",
      "deviceProfileName",
      "deviceName",
      "eui",
      "tags",
      "gatewayId",
      "uplinkId",
      "rssi",
      "snr",
      "channel",
      "rfChain",
      "location",
      "context",
      "metadata",
      "region_common_name",
      "region_config_id",
      "crcStatus",
      "frequency",
      "modulation",
      "bandwidth",
      "spreadingFactor",
      "codeRate"
    ]
  }
}